query characterDeathSaves($characterId: UUID!) {
  character(characterId: $characterId) {
    dynamicStats {
      deathSaves {
        failureCount
        successCount
      }
      isDying
      isDead
    }
  }
}
